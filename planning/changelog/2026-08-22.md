# Daily changelog — August 22, 2026

**Date:** 2026-08-22
**PRs merged:** 2

---

## Summary

A quiet day, dominated by the previous day's automated housekeeping landing plus one small nightly-audit refactor. No user-facing changes.

## What shipped

### [#1381 refactor(hooks): share the markdown trigger-file guard between summarize and rewrite](https://github.com/allenhutchison/obsidian-gemini/pull/1381)

The `audit-architecture` nightly sweep flagged a DRY violation: `HookRunner.runSummarize` and `HookRunner.runRewrite` each opened with the same eight-line preamble (cancel check, `resolveTriggerFile()`, null guard, non-markdown skip, second cancel check), differing only in the action name used in the log message. The fix extracts a private `resolveMarkdownTriggerFile(action, isCancelled)` helper and calls it from both methods. Pure extraction, no behavior change — the log output is byte-identical.

### [#1383 chore(daily): 2026-08-22 daily update](https://github.com/allenhutchison/obsidian-gemini/pull/1383)

The prior day's automated daily-update run: added `planning/changelog/2026-08-21.md` (covering 5 PRs merged 2026-08-21), a clean `audit-product-docs` pass with no drift found, and a `triage-issues` run with nothing to label. Its docs audit re-confirmed a still-open, out-of-scope code observation for maintainer follow-up: `src/main.ts`'s `DEFAULT_SETTINGS.openaiModelName` still defaults to `'gpt-5.6'` while the OpenAI catalog's actual chat-default id is `'gpt-5.6-sol'` (the sibling summary/completions defaults correctly use `-terra`/`-luna`); `reconcileOpenAI()` self-heals the mismatch on first model-list load, so docs (which already document the effective `'gpt-5.6-sol'` default) don't need a fix.
