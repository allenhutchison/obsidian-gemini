# Daily changelog — September 05, 2026

**Date:** 2026-09-05
**PRs merged:** 4

---

## Summary

A quiet day: the previous day's automated daily-update PR merged, one `audit-architecture` dead-field cleanup landed, the `auto-dev` pipeline shipped its path-containment lint rule from #1402, and Dependabot bumped `fast-uri` for a security fix.

## What shipped

### [#1475 chore(daily): 2026-09-04 daily update](https://github.com/allenhutchison/obsidian-gemini/pull/1475)

The automated daily-update run for 2026-09-04: wrote that day's changelog (6 merged PRs), ran a docs audit that found no drift, and confirmed no unlabeled open issues.

### [#1473 refactor(agent): drop the reader-less AgentLoopResult.retried flag](https://github.com/allenhutchison/obsidian-gemini/pull/1473)

The `audit-architecture` sweep flagged `AgentLoopResult.retried` as dead wiring — set on both empty-response-retry paths but read by nothing, unlike its four sibling status flags which all have a production consumer. Removed from the type, its two write sites, and the tests that asserted on it; the retry itself is still observable via the `onEmptyResponseRetry` hook and a warn log, so no coverage was lost.

### [#1472 docs(guidelines): encode the "route path containment through file-utils" rule (#1402)](https://github.com/allenhutchison/obsidian-gemini/pull/1472)

Vault path containment kept getting hand-rolled as `p.startsWith(folder + '/')` instead of calling `isPathInFolder()` — eleven-plus sites across five prior audit sweeps. This lands the prose rule in `.claude/guidelines/coding.md` plus an ESLint `no-restricted-syntax` backstop scoped to `src/` (with `file-utils.ts`, which owns the predicate, exempted), and routes the four sites the rule flagged through the shared helper. A fifth site in `skill-manager.ts` is a deliberate traversal guard and keeps a line-scoped disable instead.

Fixes #1402. Contributed by the `auto-dev` pipeline from the approved plan on that issue.

### [#1471 chore(deps): bump fast-uri from 3.1.5 to 3.1.7](https://github.com/allenhutchison/obsidian-gemini/pull/1471)

Dependency bump: `fast-uri` 3.1.5 → 3.1.7, a security release fixing high-severity authority-injection and host-confusion advisories in `serialize()` and IP-literal parsing.
