# Daily changelog — September 15, 2026

**Date:** 2026-09-15
**PRs merged:** 3

---

## Summary

Quiet day — the previous day's automated update landed, followed by two small `audit-architecture`-driven cleanups: one dead-export deletion and one duplicate label-map de-duplication in the settings UI.

## What shipped

### [#1526 chore(daily): 2026-09-14 daily update](https://github.com/allenhutchison/obsidian-gemini/pull/1526)

The automated daily-update run for September 14, covering that day's ten merged PRs, two doc-drift fixes (a removed hook reference in `AGENTS.md` and a stale project-scope claim in `docs/guide/semantic-search.md`), and no unlabeled issues to triage.

### [#1523 refactor(settings): share one feature-group label-key map](https://github.com/allenhutchison/obsidian-gemini/pull/1523)

`audit-architecture` sweep fix. The `{ text, web, media } → heading translation key` map was written out twice — once as `GROUP_HEADING_KEY` in `page-features.ts`, once as a `groupLabelKey` literal rebuilt on every loop iteration inside `topLevelFeaturesDisplay` in `display-values.ts`. Consolidates both into one exported `FEATURE_GROUP_LABEL_KEY` in `display-values.ts`, alongside the sibling `FEATURE_LABEL_KEY`/`PROVIDER_SHORT_LABEL_KEY` maps. No behavior change.

### [#1522 refactor: delete two dead exports the @public tag hid from knip](https://github.com/allenhutchison/obsidian-gemini/pull/1522)

`audit-architecture` sweep fix. `featureDisplayValue` (`src/api/provider-status.ts`) and `remoteHostForModel` (`src/models.ts`) had zero callers anywhere in the repo, but their `@public` JSDoc tag exempted them from the `npm run knip` dead-export check. `featureDisplayValue` was also a drifted second implementation of the live `featureRowDisplay`, missing the `rag`/`deepResearch` labels and the "no longer available" model case. Deletions only, no behavior change.
