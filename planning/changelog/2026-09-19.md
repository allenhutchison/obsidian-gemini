# Daily changelog — September 19, 2026

**Date:** 2026-09-19
**PRs merged:** 5

---

## Summary

A quiet, cleanup-focused day. Three PRs closed out the `#1369` tracker's remaining entries — sharing the catch-up modal's per-entry action handler, the project-picker command preamble (with one small, deliberate behavior change), and the RAG vault scanner's delete-store call. The previous day's automated daily-update PR merged, and the day closed with a larger refactor consolidating cancellable-stream lifecycle handling across the Gemini, Ollama, and OpenAI provider clients.

## What shipped

### [#1565 refactor(ui): share the CatchUpModal per-entry action handler](https://github.com/allenhutchison/obsidian-gemini/pull/1565)

Entry 3 of 5 on `#1369`. Extracts a private `wireEntryAction(primary, other, list, entry, opts)` helper holding the shared nine-step approve/skip flow in `catch-up-modal.ts` — disable both buttons, run the action, drop the entry, re-render or close, and on failure re-enable both buttons with a per-action log/notice. No behavior change; verified with a throwaway jsdom harness driving all four approve/skip success/failure paths.

### [#1568 chore(daily): 2026-09-18 daily update](https://github.com/allenhutchison/obsidian-gemini/pull/1568)

The previous day's automated daily-update run: wrote the September 18 changelog, fixed a stale `AGENTS.md` reference missing `AnthropicClient` from the API-layer factory description, and found no unlabeled issues to triage.

### [#1569 refactor(commands): share the project-picker command preamble](https://github.com/allenhutchison/obsidian-gemini/pull/1569)

Entry 4 of 5 on `#1369`. Extracts a shared `pickProject(plugin, act)` preamble for `open-project-settings` and `resume-project-session` (init check, project discovery, empty-list notice, single-project shortcut, picker). The one deliberate behavior change: the single-project shortcut now also applies to `resume-project-session`, skipping the picker when there's exactly one project, matching what `open-project-settings` already did.

### [#1570 refactor(rag): share the File Search Store delete call](https://github.com/allenhutchison/obsidian-gemini/pull/1570)

Entry 5 of 5, closing out `#1369`. Shares the `ai.fileSearchStores.delete({ force: true })` retry-wrapped call between `deleteFileSearchStore` and the `startFresh` recreation path via a small `deleteStore()` helper, while keeping each call site's distinct error handling (not-found swallowing vs. surfacing) untouched.

### [#1572 refactor(api): share the cancellable-stream lifecycle across provider clients](https://github.com/allenhutchison/obsidian-gemini/pull/1572)

Closes `#1350`. Introduces `runCancellableStream()`, a shared lifecycle (signal-first cancellation, a single `finalize()` exit for every arm, an `onCancel` escape hatch for transports without a native signal) now used by the Gemini, Ollama, and OpenAI streaming clients. Collapses a duplicated response-literal bug class in the Ollama client and closes a real cancel-before-start-resolves gap in its test coverage. No streaming semantics changed — every existing per-client streaming test passes unchanged.
