# Daily changelog — May 01, 2026

**Date:** 2026-05-01
**PRs merged:** 1

---

## Summary

Quiet day — only the automated daily-update PR landed, bringing a meaningful doc correction for scheduled-task tool access categories and the 2026-04-30 internal changelog.

## What shipped

### [#733 chore(daily): 2026-05-01 daily update](https://github.com/allenhutchison/obsidian-gemini/pull/733)

The automated daily housekeeping run for 2026-04-30. In addition to writing the prior day's changelog (`planning/changelog/2026-04-30.md`), this PR corrected a significant factual error in `docs/guide/scheduled-tasks.md`: the `enabledTools` field had been documented with `read_write` and `destructive` as valid category values, but neither exists in the `ToolCategory` enum. The enum actually defines `read_only`, `vault_ops`, `external_mcp`, `skills`, and `system`. Tasks written with the old values silently received no vault-write tool access. The docs now match what actually works in frontmatter.

A matching code bug remains open: `src/ui/scheduler-management-modal.ts` still exposes the stale `['read_only', 'read_write', 'destructive']` values in its checkbox UI — that will need a separate code + UI fix. Issue triage found no unlabeled open issues.
