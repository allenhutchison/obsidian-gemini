# Daily changelog — June 18, 2026

**Date:** 2026-06-18
**PRs merged:** 3

---

## Summary

Architecture-audit housekeeping dominated the day: two nightly-sweep refactors landed back-to-back — removing dead notification scaffolding from the tool execution engine and tightening a type guard in the file picker — followed by the routine daily-update PR wrapping up June 17. No user-visible behavior changes; purely internal code quality work.

## What shipped

### [#992 refactor: remove dead notification scaffolding in execution-engine](https://github.com/allenhutchison/obsidian-gemini/pull/992)

Cleared out an inert `executionNotice` dummy object and its two `.hide()` callsites from `src/tools/execution-engine.ts`. The Notice had been disabled when tool results moved to the chat UI during an earlier refactor, but the scaffolding was left behind "for compatibility" even though nothing referenced it. The catch block's error handling was also collapsed into a single return for consistency. No behavior change; no public API surface touched.

### [#993 refactor: type-guard file-picker fuzzy-search filter](https://github.com/allenhutchison/obsidian-gemini/pull/993)

Replaced a plain `.filter(({ result }) => result !== null)` in `src/ui/agent-view/file-picker-modal.ts` with a user-defined type guard so TypeScript can narrow the `result` type without the two `result!` non-null assertions that followed. The sort now accesses `b.result.score` directly. Same predicate, same ordering — pure typing tightening flagged by the nightly architecture-audit sweep.

### [#995 chore(daily): 2026-06-17 daily update](https://github.com/allenhutchison/obsidian-gemini/pull/995)

Routine daily-update run for June 17. Wrote `planning/changelog/2026-06-17.md` (1 PR: the June 16 daily update). Docs audit found no drift; triage found no unlabeled issues. Quiet Tuesday.
