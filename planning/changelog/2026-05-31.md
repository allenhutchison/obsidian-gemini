# Daily changelog — May 31, 2026

**Date:** 2026-05-31
**PRs merged:** 5

---

## Summary

Housekeeping day. The nightly architecture-audit sweep landed a small but principled compiler-directive cleanup, a batch of dependency bumps (including the Obsidian API to 1.13.0 and `@google/genai` to 2.7.0), and two deferred daily-update PRs that had been queued from earlier in the week.

## What shipped

### [#919 chore(daily): 2026-05-31 daily update](https://github.com/allenhutchison/obsidian-gemini/pull/919)

Automated housekeeping PR for May 31. Covered a quiet May 30 (no merges), a clean docs audit, and an empty triage pass. Added `planning/changelog/2026-05-30.md` as an empty-day signal per the daily-changelog convention.

### [#917 refactor: use @ts-expect-error for inline-attachment internal-API access](https://github.com/allenhutchison/obsidian-gemini/pull/917)

Architecture-audit nightly sweep flagged the one remaining `@ts-ignore` in `src/ui/agent-view/inline-attachment.ts:96`, where every other Obsidian internal-API access site in the codebase already uses `@ts-expect-error`. The swap matters because `@ts-expect-error` fails the build if the suppression ever becomes unnecessary (e.g. Obsidian's public typings gain a `getConfig` signature), while `@ts-ignore` silently stays behind as dead noise. No behavior change — pure compiler-directive swap.

### [#916 chore(deps-dev): bump the dev-dependencies group with 6 updates](https://github.com/allenhutchison/obsidian-gemini/pull/916)

Dependabot batch bump: `@eslint/json` 1.2.0 → 2.0.0 (breaking change: adds `meta.languages` to JSON rules), `@google/genai` 2.6.0 → 2.7.0, `eslint` 10.4.0 → 10.4.1, `lint-staged` 17.0.5 → 17.0.6, `obsidian` 1.12.3 → 1.13.0, `typescript-eslint` 8.59.4 → 8.60.0. The Obsidian API and `@google/genai` bumps are the most significant — worth checking for new APIs or deprecations in follow-up work.

### [#915 chore(deps): bump actions/attest-build-provenance from 2 to 4](https://github.com/allenhutchison/obsidian-gemini/pull/915)

Dependabot bump of the CI build-provenance action from v2 to v4. As of v4, `actions/attest-build-provenance` is a thin wrapper over `actions/attest`; existing usage is unaffected.

### [#914 chore(daily): 2026-05-29 daily update](https://github.com/allenhutchison/obsidian-gemini/pull/914)

Deferred daily housekeeping PR for May 29. Added the May 29 changelog (2 PRs: configurable headless tool-iteration cap #909 and the May 28 daily-update #911) and patched 3 doc-drift items introduced by PR #909 — `AGENTS.md` was missing `edit_skill` from the agent skills tool list, and `README.md`'s scheduled-tasks and lifecycle-hooks bullets omitted the new `maxIterations` frontmatter key.
