# Daily changelog — July 27, 2026

**Date:** 2026-07-27
**PRs merged:** 2

---

## Summary

Quiet day — one mechanical cleanup surfaced by the architecture audit, plus the previous day's automated daily-update run landing after review.

## What shipped

### [#1258 refactor(settings): remove dead re-export of SettingsSectionContext](https://github.com/allenhutchison/obsidian-gemini/pull/1258)

The `audit-architecture` nightly sweep flagged `src/ui/settings.ts:12` as re-exporting `SettingsSectionContext` from `./settings-helpers` with no consumer importing it from `settings.ts` — every settings section (`settings-general`, `settings-tools`, `settings-mcp`, `settings-rag`, `settings-agent-config`) already imports the type directly from `settings-helpers`. `npm run knip` reported it as the repo's only unused export. This PR deletes the vestigial re-export and keeps the separate local `import type` still in use at `settings.ts:41`. Type-only change with no bundle impact.

### [#1259 chore(daily): 2026-07-26 daily update](https://github.com/allenhutchison/obsidian-gemini/pull/1259)

The prior day's automated housekeeping run, merged after review: wrote the 2026-07-26 changelog entry, fixed two documentation drift items (a stale description of the inert "Allow System Prompt Override" toggle in the README, and a leftover `test-scripts/` reference in `AGENTS.md` that should have pointed at the `evals/` harness), and found no unlabeled issues to triage.
