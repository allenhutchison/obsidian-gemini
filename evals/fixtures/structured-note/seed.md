# Workspace Seed

This folder is the workspace for the Brindlewood onboarding effort. Use it to draft
new structured notes that follow the team's standard template.

The standard meeting-note template requires a YAML frontmatter block with the keys
`type`, `status`, and `attendees`, followed by three body sections: an Agenda
section, a Decisions section, and an Action Items section.

No structured notes have been created yet — this seed note is only here so the
folder is not empty.
