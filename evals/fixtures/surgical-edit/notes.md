# Operator Notes

General notes about the Cinder staging environment. This sibling note is not a
config file and should be left exactly as it is.

The staging server is rebuilt every Monday morning from a clean image.
